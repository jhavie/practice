
const tableData = function(table, init = {}) {
    if (table) {
        throw new Error('table have required!!!');
    }

    this.table = table;
    this.$data = {
        totalBorder:　false,

    };
}

tableData.prototype.setData = function () {

}

tableData.prototype

export default tableData;